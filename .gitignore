# Secrets — never commit
.env
*.env
!*.env.example

# Terraform (also ignored in infra/terraform/.gitignore)
**/*.tfstate
**/*.tfstate.*
**/.terraform/
**/terraform.tfvars
**/kubeconfig

# Builds / deps
node_modules/
target/
dist/
build/

.DS_Store
infra/terraform/.terraform.lock.hcl
